spring:
  profiles:
    active: prod


---
redis-server:
  list :
    - alias: server1alias
      hosts : server1host
      password : server1password
      port : server1port
    - alias: server2alias
      hosts : server2hosts
      password : server2password
      port : server1port
